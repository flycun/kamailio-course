# 管理入站响应路由
# 当收到SIP响应消息时触发此路由，用于处理响应消息
onreply_route[MANAGE_REPLY] {
	# 记录收到响应消息到调试日志
	xdbg("incoming reply\n");
	
	# 设置消息方向标志
	# 重新确定响应消息的来源方向(核心网络或公共网络)
	route(SET_DIRECTION_FLAG);
	
	# 处理RTP响应
	# 根据响应消息设置相应的RTP媒体流处理
	route(SET_RTP_REPLY);
}