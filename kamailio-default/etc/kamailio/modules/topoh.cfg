loadmodule "topoh.so"

# 拓扑隐藏模块配置 - 头部字段层面的拓扑隐藏
# 用于隐藏SIP消息头部中的网络拓扑信息

modparam("topoh", "mask_key", "SuperSecretKey") # 设置用于加密头部字段的密钥
modparam("topoh", "mask_ip", "192.168.254.2") # 设置用于掩码的公共IP地址
#modparam("topoh", "mask_callid", 1) # 可选：对Call-ID进行掩码处理(默认已通过topos模块处理)

